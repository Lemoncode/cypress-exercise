import React from 'react';
import { AppLayout } from 'layouts';
import { TaskEditContainer } from 'pods/task-edit/task-edit.container';

export const TaskEditScene = () => (
  <AppLayout>
    <TaskEditContainer />
  </AppLayout>
);
