import React from 'react';
import { AppLayout } from 'layouts';
import { TaskContainer } from 'pods/tasks/tasks.container';

export const TasksScene = () => (
  <AppLayout>
    <TaskContainer />
  </AppLayout>
);
